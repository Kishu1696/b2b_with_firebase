import AuthForm from "@/pages/Auth/AuthForm";

export default function Register() {
  return <AuthForm mode="register" title="Create your workspace" subtitle="Launch an AI-assisted liquidation command center." />;
}
