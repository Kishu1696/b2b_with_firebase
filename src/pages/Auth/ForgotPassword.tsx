import AuthForm from "@/pages/Auth/AuthForm";

export default function ForgotPassword() {
  return <AuthForm mode="forgot" title="Recover access" subtitle="Receive a secure reset workflow for your operator account." />;
}
