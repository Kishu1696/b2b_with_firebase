import AuthForm from "@/pages/Auth/AuthForm";

export default function Login() {
  return <AuthForm mode="login" title="Welcome back" subtitle="Sign in to command inventory liquidation operations." />;
}
