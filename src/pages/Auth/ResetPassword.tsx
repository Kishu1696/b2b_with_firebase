import AuthForm from "@/pages/Auth/AuthForm";

export default function ResetPassword() {
  return <AuthForm mode="reset" title="Set a new password" subtitle="Choose a strong password for continued secure access." />;
}
